{
  "version": 2,
  "builds": [
    {
      "src": "backend/src/api/index.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": ["backend/dist/**"] }
    },
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "frontend/dist" }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/src/api/index.ts" },
    { "src": "/(.*)", "dest": "frontend/dist/$1" }
  ]
}
